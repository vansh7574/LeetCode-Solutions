/* Write your T-SQL query statement below */
select contest_id, Round(100.0 * Count(*)/(Select count(*) from Users),2) as percentage from Register group by contest_id order by percentage desc, contest_id ASC